// Phase 3B — Form CRUD route tests (not yet implemented)
import { describe, it } from "vitest";

describe("Form routes", () => {
  it.todo("POST /api/forms — creates a form");
  it.todo("GET /f/:id — serves form HTML");
  it.todo("DELETE /api/forms/:id — deletes a form");
});
