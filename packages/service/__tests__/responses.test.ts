// Phase 3B — Response collection route tests (not yet implemented)
import { describe, it } from "vitest";

describe("Response routes", () => {
  it.todo("POST /api/responses/:formId — submits a response");
  it.todo("GET /api/responses/:formId — lists responses");
  it.todo("GET /api/responses/:formId/xlsx — exports as Excel");
});
