name = "formant"
main = "src/worker.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[[d1_databases]]
binding = "DB"
database_name = "formant-db"
database_id = ""

# Connect Google Sheet — set via: wrangler secret put GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET
